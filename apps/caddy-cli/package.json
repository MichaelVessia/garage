{
  "name": "@garage/caddy-cli",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "caddy": "dist/caddy"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "bun run clean && bun build src/main.ts --compile --outfile dist/caddy",
    "typecheck": "tsgo --noEmit",
    "lint": "oxlint --config ../../oxlint.config.mjs src/ test/",
    "lint:fix": "oxlint --config ../../oxlint.config.mjs --fix src/ test/",
    "format": "oxfmt --config ../../oxfmt.config.mjs --check src/ test/ package.json tsconfig.json tsconfig.build.json vitest.config.ts",
    "format:fix": "oxfmt --config ../../oxfmt.config.mjs src/ test/ package.json tsconfig.json tsconfig.build.json vitest.config.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@effect/platform-bun": "4.0.0-beta.64",
    "@garage/cli-protocol": "workspace:*",
    "@garage/caddy": "workspace:*",
    "effect": "^4.0.0-beta.64"
  }
}
